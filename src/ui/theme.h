#pragma once

// Estilo visual del panel de control Dear ImGui.
namespace ui {

// Tema oscuro con esquinas redondeadas y acentos azules.
void ApplyObsidianTheme();

// Marcador "(?)" con un tooltip de ayuda al pasar el ratón.
void HelpMarker(const char* description);

} // namespace ui
